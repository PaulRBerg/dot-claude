{
  // Read permissions - allowed and denied file read operations
  // NOTE: to add directories, use the additional-dirs.jsonc file
  "permissions": {
    "allow": ["Read(~/.vimrc)", "Read(~/.zshrc)"],
    "deny": ["Read(./.env)", "Read(./.env.production)"]
  }
}
