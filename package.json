{
  "name": "@prb/dot-claude",
  "description": "PRB's .claude directory.",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "lint-staged": "lint-staged --concurrent false --no-stash --no-hide-partially-staged",
    "prepare": "test \"$CI\" = 'true' || (husky && bash helpers/merge_settings.sh)",
    "pre-commit": "just pre-commit"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "json5": "^2.2.3",
    "lint-staged": "^17.3.0",
    "prettier": "^3.9.6"
  }
}
