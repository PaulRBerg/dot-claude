{
  "name": "@prb/dot-claude",
  "description": "PRB's .claude directory.",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "prepare": "test \"$CI\" = 'true' || (husky && bash helpers/merge_settings.sh)"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "json5": "^2.2.3",
    "lint-staged": "^16.2.7"
  }
}
