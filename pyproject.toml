[project]
  dependencies = [
    "pytest",
    "pyright",
    "ruff",
    "notify-py>=0.3.43",
    "loguru>=0.6.0",
    "pydantic>=2.12.4",
    "pydantic-settings>=2.12.0",
]
  description = "Root .claude directory."
  name = "dot-claude"
  requires-python = ">=3.12"
  version = "1.0.0"

[dependency-groups]
dev = [
    "pytest-mock>=3.15.1",
]
